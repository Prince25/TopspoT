<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prince.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".ListViewPictures" />
        <activity android:name=".Activities" />
        <activity android:name=".ClassroomList" />
        <activity android:name=".bh3400imageActivity"></activity>
        <activity android:name=".math5200imageActivity"></activity>
        <activity android:name=".moore100imageActivity"></activity>
    </application>

</manifest>